name: pathar_fe
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^17.3.0
  provider: ^6.1.5+1
  dio: ^5.9.2
  flutter_dotenv: ^6.0.1
  geolocator: ^14.0.2
  permission_handler: ^12.0.3
  camera: ^0.12.0+1
  google_mlkit_object_detection: ^0.15.0
  google_mlkit_image_labeling: ^0.14.2
  flutter_tts: ^4.2.5
  sensors_plus: ^7.0.0
  flutter_compass: ^0.8.1
  flutter_secure_storage: ^9.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1

  # Google Sign-In (Firebase lo usa internamente)
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - .env